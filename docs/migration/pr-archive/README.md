# PR Archive Skeleton

GitHub PR content was not available during the local archive attempt. These files preserve known PR numbers, topics, and manual recovery needs for migration follow-up.

Attempted read path: unauthenticated GitHub API via `curl`.

Result: unavailable.
