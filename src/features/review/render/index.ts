// src/features/review/render/index.ts — Lane D (D7) public API.
//
// Per-language display utilities for the review card UI. Imported by D5
// (ui/session) and D6 (ui/overview). Pure + framework-agnostic.

export {
  scriptClass,
  fontSizeClass,
  langTag,
  pronunciationLabel,
} from "./displayUtils";
