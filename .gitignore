# secrets
.env
.env.*
!.env.example
ANON_KEY*
*.key
*.pem

# build
dist/
.vite/
.next/
.turbo/
.parcel-cache/

# build-generated artifacts (regenerated by prebuild hooks)
public/audio/manifest.json
public/version.json

# deps
node_modules/

# OS / editor
.DS_Store
.eslintcache

# local scratch
scripts/_scratch/
scripts/.user-id-tables.json
docs/*_LOG.md
bad.txt
.claude/worktrees/agent-*/
.claude/*.lock
.claude/scheduled_tasks.lock
room_scan_report.json
orphan_candidates.txt
orphan_*.txt
tmp_*.txt

# local staging (do not ship)
private-content/
.vercel
mobile-web/
dist/
/assets/

.backup-mobile/
generate_audio.py
scan_missing_audio.py
**/.DS_Store

# Capacitor native projects are source-of-truth for store build metadata.
# Keep project metadata tracked; ignore generated assets, dependency
# sandboxes, build outputs, local IDE state, and signing secrets.
ios/App/App/public/
ios/App/Pods/
ios/App/build/
ios/App/DerivedData/
ios/App/**/*.xcuserstate
ios/App/**/*.xcuserdata/

android/.gradle/
android/.idea/
android/**/build/
android/app/release/
android/app/src/main/assets/public/
android/local.properties
android/keystore.properties

# Editor backup files
*.bak

# Playwright output
playwright-report/
playwright-smoke-report/
playwright-prod-smoke-report/
playwright-crawler-report.json
test-results/

# TTS sample output (regenerated by generate-listening-audio.js + generate-elevenlabs-audio.js)
audio-output/

# Audio pipeline manifest + progress (regenerated by scripts/build-audio-manifest.ts)
audio-manifest.json
audio-progress.json
.deepseek/
audit_tmp/
reports/chau-report-*.md
reports/FACTORY_REPORT_FOR_CHATGPT.md
node_modules

# Supabase CLI local state (machine-generated, env-specific; matches `supabase init` convention)
supabase/.temp

# Storybook build output (Lane E)
storybook-static

# k6 load test reports (auto-generated; too large for git)
reports/load/

# Step-19 self-eval harness outputs (produced at runtime; CI artifact, not tracked)
_eval_out/

# ── Factory / cell-os runtime scratch (A4 tree cleanup 2026-07-08) ──
# Per-machine runtime artifacts & state regenerated by the factory/cell-os
# scripts. NOT deliverables — kept on disk locally, never tracked.
artifacts/
reports/runtime-artifacts/
state/audio_coverage/
state/packets/

# One-off GitLab pipeline operational logs (point-in-time ops output, not
# living deliverables — the pipelines they describe are already gone).
reports/ZOMBIE_PIPELINES_CANCEL_REPORT.md
reports/ZOMBIE_PIPELINES_DELETE_REPORT.md
reports/ZOMBIE_PIPELINES_DELETE_REMAINING_REPORT.md
reports/ZOMBIE_PIPELINES_DELETE_REMAINING_2_REPORT.md
reports/HANGING_PIPELINES_FIX_REPORT.md
reports/LIVE_PIPELINES_INSPECTION_REPORT.md
reports/DOC_CONSOLIDATOR_REPORT.md

# One-off pipeline-ops shell scripts (throwaway; targeted already-deleted pipelines).
scripts/cancel-zombie-pipelines-admin.sh
scripts/delete-zombie-pipelines-admin.sh
scripts/fix-hanging-pipelines-admin.sh
scripts/inspect-live-pipelines-admin.sh

# Generated factory/control-plane reports (reproducible from tracked scripts/*.mjs;
# snapshots regenerate on each run — track the generators, not their output).
reports/FACTORY_PACKET_OS_REPORT.md
reports/FACTORY_PACKET_JUDGE_REPORT.md
reports/FACTORY_PACKET_GAP_WP_REPORT.md
reports/FACTORY_PACKET_RELEASE_GATE_REPORT.md
reports/FACTORY_PACKET_RUNTIME_OBSERVER_REPORT.md
reports/FACTORY_PACKET_RUNTIME_ARTIFACT_CAPTURE_REPORT.md
reports/FACTORY_PACKET_RUNTIME_ARTIFACT_INSPECTION_REPORT.md
reports/CELL_OS_ENGINEERING_REPORT.md
reports/CELL_OS_SOURCE_INVENTORY.md
reports/CAPABILITY_CONTROL_REPORT.md
reports/AUDIO_COVERAGE_REPORT.md
reports/MISSING_AUDIO_WORKPACKS.md

# Per-machine cell-os runtime DB (never committed on any branch; regenerated by
# scripts/cell-os/*.mjs). Kept on disk locally, never tracked.
state/cell_os_engineering.sqlite3
# Note: state/dp_int_factory.sqlite3 (tracked, canonical-shared) is deliberately
# NOT ignored — its ledger commits are the multi-machine source of truth; its
# pending change is committed separately once the factory idles.

# Wrangler local build artifacts (generated worker bundles, tmp)
.wrangler/

# ── Fake-factory apparatus + generated reports (chore/untrack-factory-reports) ──
# The c3-factory/ and cell-os/ apparatus and every reports/c3-* output are
# machine-local scratch that touched zero product files (91/91 workpacks were
# self-validating PASS proofs). Never deliverables — kept on disk, never tracked.
# Rationale: docs/postmortems/2026-07-08-fake-factory.md and C3-STRATEGY.md LAW 10.
# (Anchored to repo root so tracked scripts/cell-os/** and scripts/factory/** are
#  unaffected.)
/c3-factory/
/cell-os/
/reports/c3-factory/
/reports/c3-*
/reports/canonical-cell-map-validation.latest.json
