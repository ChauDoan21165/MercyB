# .gitlab-ci.yml
#
# GitLab CI for MercyB. Two concerns live here:
#
#   1. **`test` stage** — pull-request and push-to-main gates that
#      mirror `.github/workflows/ci.yml`. Until this port, the GitLab
#      pipeline only ran the `backup` stage on schedules, so every
#      "pipeline green" badge on a merge request was a trivial empty-
#      pipeline pass. The `.github/workflows/ci.yml` file is kept on
#      disk for reference until this port is proven over a few MRs.
#
#   2. **`backup` stage** — nightly external Postgres dumps (preserved
#      verbatim from the prior version of this file). See
#      `scripts/db-backup/README.md` for the full design.
#
# Job naming follows the same mapping as the GitHub workflow:
#   - `build`              → "Build and Test" in GitHub
#   - `lint`               → "Lint Code"
#   - `validate-rooms`     → "Validate Rooms"
#   - `module-boundaries`  → "Module Boundaries"  (informational, not yet
#                            in any required-checks ruleset — see memory
#                            `project_ci_workflow_consolidation`)
#
# Things deliberately NOT ported:
#   - GitHub Actions specifics (`actions/checkout@v4`, `actions/setup-node@v4`).
#     GitLab clones the project automatically and the image carries Node.
#   - Vercel-specific deploy steps. Netlify auto-deploys via its own
#     GitLab integration; CI does not deploy.
#   - Other workflows under `.github/workflows/` (performance-ci,
#     mutation-testing, lighthouse, etc.). They moved to schedule /
#     workflow_dispatch / push:main in the GitHub consolidation and
#     belong to follow-up MRs.

stages:
  - test
  - deploy
  - backup

# ── Global CI tuning (Lane C: faster feedback) ─────────────────────────
#
# The local Mac runner can run multiple jobs at once. Keep each job in a
# job-specific checkout so concurrent get_sources/cleanup steps cannot corrupt
# a shared worktree. The local shell runner has shown collisions even under the
# runner-slot path, so include both pipeline and job ids. The path must stay
# under $CI_BUILDS_DIR.
#
# The remaining knobs trim the cache archive/extract tax that wraps each job
# (a trace showed ~15s to save + ~6s to restore the node_modules cache, per job):
#   - FF_USE_FASTZIP            parallel zip impl; the large node_modules
#                               cache (~1756 pkgs) archives far faster
#                               than the default gzip path.
#   - CACHE_COMPRESSION_LEVEL   `fast` trades a little cache size for
#                               speed. The cache is local-only on this
#                               runner ("stored only locally"), so the
#                               extra bytes cost nothing.
#   - TRANSFER_METER_FREQUENCY  progress logging cadence only; no
#                               behaviour change.
#   - GIT_DEPTH                 bounded shallow clone. CI's only git usage
#                               (status/diff/ls-files in the registry-drift
#                               and check-no-raw-react-lazy steps) reads
#                               the working tree, never deep history.
variables:
  FF_USE_FASTZIP: "true"
  CACHE_COMPRESSION_LEVEL: "fast"
  TRANSFER_METER_FREQUENCY: "2s"
  GIT_DEPTH: "20"
  GIT_CLONE_PATH: "$CI_BUILDS_DIR/$CI_PIPELINE_ID/$CI_JOB_ID/$CI_PROJECT_PATH_SLUG"

# ── Shared anchors for the `test` stage ────────────────────────────────
#
# `node:24-bookworm-slim` is the GitLab CI base image. Debian-slim is
# wider-compatibility than Alpine for the project's native deps; the
# size cost (~150 MB) is paid once per runner cache.

# Reusable npm cache definition (key + paths only). Each job picks a
# `policy`. The key tracks package-lock.json so a lockfile change
# invalidates the cache and forces a fresh install.
#
# Producer/consumer split: `build` runs first and is the cache PRODUCER
# (`pull-push`). The lighter jobs (lint / validate-rooms /
# module-boundaries) are CONSUMERS (`pull`) — they still run `npm ci`, so
# a cache miss is always safe; they simply stop re-archiving an identical
# node_modules (~15s each, per the trace). On a `concurrent = 1` runner
# those three saves are pure serial dead time, so dropping them comes off
# the pipeline wall-clock directly. Shared-runner fallback jobs keep
# `pull-push` (the anchor default) because they run on a different runner
# with its own cold local cache and must seed it themselves.
.npm_cache_def: &npm_cache_def
  key:
    files:
      - package-lock.json
  paths:
    - node_modules/
    - .npm/

.node_job: &node_job
  image: node:24-bookworm-slim
  interruptible: true
  cache:
    <<: *npm_cache_def
    policy: pull-push
  before_script:
    # [B3 builds-reaper] Bound disk on the macOS shell runners. Every job gets a
    # unique GIT_CLONE_PATH ($CI_BUILDS_DIR/$CI_PIPELINE_ID/$CI_JOB_ID/...) and the
    # runner never auto-cleans custom build dirs, so full checkouts + their
    # node_modules accumulate until disk fills. Numeric-name match scopes this to
    # MercyB's GIT_CLONE_PATH layout (won't touch other projects' default build
    # dirs) and we NEVER touch the current pipeline.
    #
    # Two prune triggers (a single day of pipelines filled the small-disk
    # runner-2 / "the Air" overnight under the old 24h age — down to 119Mi free):
    #   1. AGE — prune non-current pipeline dirs older than REAP_AGE_MIN MINUTES
    #      (default 120 ≈ 2h). Uses `find -mmin`, so the knob is minutes, not the
    #      old -mtime days (2h isn't expressible in -mtime). Tune via REAP_AGE_MIN.
    #   2. DISK PRESSURE — if the builds volume is at/above REAP_FULL_PCT used%
    #      (default 90), ignore the age floor but only reap pipeline dirs that do
    #      NOT contain a recent active-job marker. This prevents a newer job from
    #      deleting an older still-running job's current working directory, which
    #      shows up as uv_cwd/ENOENT during npm ci. Markers older than
    #      REAP_ACTIVE_MARKER_MAX_MIN (default 360min, well past the CI timeout)
    #      are treated as stale so killed jobs do not pin disk forever.
    #
    # DRY-RUN BY DEFAULT — this is the first destructive filesystem op on CI
    # infra, so it earns one clean dry-run: it logs CI_BUILDS_DIR, the resolved
    # trigger, and every target it WOULD remove, and fires NO rm. After Chau
    # eyeballs that CI_BUILDS_DIR resolved correctly and every target is genuinely
    # a stale/non-current pipeline dir, flip to live by setting CI/CD variable
    # REAP_LIVE=1 (or changing the default below).
    - |
      if [ -n "$CI_BUILDS_DIR" ] && [ "$(uname -s)" = "Darwin" ]; then
        ACTIVE_MARKER="$CI_PROJECT_DIR/.ci-active-job"
        printf 'job=%s\npipeline=%s\nstarted=%s\n' "$CI_JOB_ID" "$CI_PIPELINE_ID" "$(date -u +%Y-%m-%dT%H:%M:%SZ)" > "$ACTIVE_MARKER"
        trap 'rm -f "$ACTIVE_MARKER"' EXIT
        REAP_AGE_MIN="${REAP_AGE_MIN:-120}"
        REAP_FULL_PCT="${REAP_FULL_PCT:-90}"
        REAP_ACTIVE_MARKER_MAX_MIN="${REAP_ACTIVE_MARKER_MAX_MIN:-360}"
        REAP_MODE="dry-run"; [ "${REAP_LIVE:-0}" = "1" ] && REAP_MODE="live"
        AVAIL_BYTES="$(df -Pk "$CI_BUILDS_DIR" 2>/dev/null | awk 'NR==2 {print $4 * 1024}')"
        USED_PCT="$(df -P "$CI_BUILDS_DIR" 2>/dev/null | awk 'NR==2 {gsub(/%/,"",$5); print $5}')"
        if [ -n "$USED_PCT" ] && [ "$USED_PCT" -ge "$REAP_FULL_PCT" ]; then
          REAP_TRIGGER="disk-pressure(${USED_PCT}%>=${REAP_FULL_PCT}%)"; FIND_AGE="-mmin +0"
        else
          REAP_TRIGGER="age(>${REAP_AGE_MIN}min)"; FIND_AGE="-mmin +$REAP_AGE_MIN"
        fi
        echo "[B3 builds-reaper] CI_BUILDS_DIR=$CI_BUILDS_DIR current=$CI_PIPELINE_ID used=${USED_PCT:-?}% avail_bytes=${AVAIL_BYTES:-?} trigger=$REAP_TRIGGER mode=$REAP_MODE runner=${CI_RUNNER_DESCRIPTION:-unknown}"
        STALE="$(
          find "$CI_BUILDS_DIR" -mindepth 1 -maxdepth 1 -type d -name '[0-9]*' ! -name "$CI_PIPELINE_ID" $FIND_AGE -print 2>/dev/null |
          while IFS= read -r d; do
            if find "$d" -mindepth 3 -maxdepth 3 -name .ci-active-job -type f -mmin "-$REAP_ACTIVE_MARKER_MAX_MIN" -print -quit 2>/dev/null | grep -q .; then
              echo "[B3 builds-reaper] skip active pipeline dir: $d" >&2
            else
              echo "$d"
            fi
          done
        )"
        if [ -z "$STALE" ]; then
          echo "[B3 builds-reaper] no prunable pipeline build dirs (trigger=$REAP_TRIGGER) — nothing to do"
        elif [ "$REAP_MODE" = "live" ]; then
          echo "[B3 builds-reaper] LIVE — removing pipeline build dirs (trigger=$REAP_TRIGGER):"
          echo "$STALE"
          echo "$STALE" | while IFS= read -r d; do [ -n "$d" ] && rm -rf -- "$d"; done 2>/dev/null || true
        else
          echo "[B3 builds-reaper] DRY-RUN — would remove the following (NO rm fired; set REAP_LIVE=1 to enable):"
          echo "$STALE"
        fi
      fi
    # `--legacy-peer-deps` mirrors the GitHub workflow's flag — the
    # repo's peer-dep tree currently requires it.
    - sleep $((RANDOM % 60))
    - npm ci --legacy-peer-deps --cache .npm --prefer-offline

# Local runner jobs are explicitly tagged so the Mac runner can keep
# "run untagged jobs" disabled. Shared-runner fallbacks are separate
# manual jobs with no tags and only appear when a pipeline is started
# with MERCYB_SHARED_RUNNER_FALLBACK=1.
#
# `isolated-builds` routes these jobs to the runner configured with
# [runners.custom_build_dir] enabled, which is required for GIT_CLONE_PATH.

.local_runner: &local_runner
  tags:
    - local
    - mac
    - mercyb
    - isolated-builds
  hooks:
    pre_get_sources_script:
      - |
        if [ "${MERCYB_PRECHECKOUT_REAPER_DISABLE:-0}" != "1" ] && [ "${CI_PIPELINE_SOURCE:-}" = "push" ] && [ "${CI_COMMIT_BRANCH:-}" = "main" ] && [ -n "${CI_BUILDS_DIR:-}" ] && [ "$(uname -s)" = "Darwin" ]; then
          REAPER_ROOT="$CI_BUILDS_DIR"
          while [ ! -d "$REAPER_ROOT" ] && [ "$REAPER_ROOT" != "/" ]; do
            REAPER_ROOT="$(dirname "$REAPER_ROOT")"
          done
          if [ "$(basename "$REAPER_ROOT")" = "gitlab-runner-builds" ]; then
            echo "[post-merge-precheckout-reaper] before df -h /"
            df -h / | sed 's/^/[post-merge-precheckout-reaper] /'
            echo "[post-merge-precheckout-reaper] root=$REAPER_ROOT current_pipeline=${CI_PIPELINE_ID:-unknown}"
            find "$REAPER_ROOT" -mindepth 1 -maxdepth 1 -type d -name '[0-9]*' -print 2>/dev/null | sort | while IFS= read -r d; do
              [ -n "$d" ] || continue
              if [ "$(basename "$d")" = "${CI_PIPELINE_ID:-}" ]; then
                echo "[post-merge-precheckout-reaper] SKIP current-pipeline path=$d"
                continue
              fi
              if command -v lsof >/dev/null 2>&1 && lsof -t +D "$d" >/dev/null 2>&1; then
                echo "[post-merge-precheckout-reaper] SKIP active-process path=$d"
                continue
              fi
              echo "[post-merge-precheckout-reaper] REMOVE path=$d safe=non-current-runner-pipeline-dir"
              rm -rf -- "$d" || echo "[post-merge-precheckout-reaper] WARN remove-failed path=$d"
            done
            echo "[post-merge-precheckout-reaper] after df -h /"
            df -h / | sed 's/^/[post-merge-precheckout-reaper] /'
          else
            echo "[post-merge-precheckout-reaper] SKIP unapproved-root path=$REAPER_ROOT"
          fi
        fi
        DISK_FLOOR_KB="${MERCYB_CI_MIN_FREE_KB:-5242880}"
        TARGET="${CI_BUILDS_DIR:-$PWD}"
        while [ ! -d "$TARGET" ] && [ "$TARGET" != "/" ]; do
          TARGET="$(dirname "$TARGET")"
        done
        FREE_KB="$(df -Pk "$TARGET" 2>/dev/null | awk 'NR==2 {print $4}')"
        if [ -z "$FREE_KB" ]; then
          echo "ERROR: insufficient-disk guard could not read free space for build volume target '$TARGET'." >&2
          exit 1
        fi
        if [ "$FREE_KB" -lt "$DISK_FLOOR_KB" ]; then
          FREE_GB="$(awk "BEGIN {printf \"%.2f\", $FREE_KB/1048576}")"
          FLOOR_GB="$(awk "BEGIN {printf \"%.2f\", $DISK_FLOOR_KB/1048576}")"
          echo "ERROR: insufficient disk before git checkout on '$TARGET': ${FREE_GB}GiB free, floor is ${FLOOR_GB}GiB. Aborting before sources are fetched to avoid mid-checkout failure." >&2
          exit 1
        fi
        FREE_GB="$(awk "BEGIN {printf \"%.2f\", $FREE_KB/1048576}")"
        echo "Disk guard OK before git checkout: ${FREE_GB}GiB free on '$TARGET'."

# Changes that affect the full app build/test path. Docs-only changes do
# not need the heavy build job on merge-request pipelines; protected
# branches still run the full validation below regardless of diff shape.

.app_validation_changes: &app_validation_changes
  - package.json
  - package-lock.json
  - tsconfig*.json
  - vite.config.*
  - vitest.config.*
  - index.html
  - src/**/*
  - public/**/*
  - scripts/**/*
  - tests/**/*
  - supabase/functions/**/*
  - supabase/config.toml
  - .gitlab-ci.yml

.lint_validation_changes: &lint_validation_changes
  - package.json
  - package-lock.json
  - eslint.config.*
  - tsconfig*.json
  - src/**/*
  - scripts/**/*
  - tests/**/*
  - .gitlab-ci.yml

.room_validation_changes: &room_validation_changes
  - package.json
  - package-lock.json
  - scripts/validate-rooms*
  - scripts/validate-data-files.js
  - scripts/validate-room-registry.js
  - scripts/validate-kids-rooms.js
  - scripts/generate-room-registry.mjs
  - src/data/**/*
  - src/lib/room*.ts
  - src/lib/room*/**/*
  - src/lib/rooms/**/*
  - src/lib/roomDataImports.ts
  - src/lib/roomManifest.ts
  - .gitlab-ci.yml

.module_validation_changes: &module_validation_changes
  - package.json
  - package-lock.json
  - dependency-cruiser.*
  - .dependency-cruiser.*
  - src/**/*
  - scripts/**/*
  - .gitlab-ci.yml

.ai_tutor_golden_flow_changes: &ai_tutor_golden_flow_changes
  - package.json
  - package-lock.json
  - vite.config.*
  - vitest.config.*
  - src/lib/tutor/**/*
  - src/lib/speak/**/*
  - src/lib/speech/**/*
  - src/lib/grammar/**/*
  - src/lib/logic/**/*
  - src/lib/ai-tutor/**/*
  - src/components/ai-tutor/**/*
  - src/components/languages/AITutorCtaBanner.tsx
  - src/components/languages/__tests__/AITutorCtaBanner.test.tsx
  - src/components/mercy-guide/hooks/useAITutor.ts
  - src/components/mercy-guide/logic/**/*
  - src/components/mercy-guide/tabs/AITutorTab.tsx
  - src/components/audio/TalkingFacePlayButton.tsx
  - src/components/teacher-mercy/TeacherMercyVoiceControls.tsx
  - src/hooks/usePronunciationRecorder.ts
  - src/pages/AiTutor.tsx
  - src/pages/__tests__/AiTutor*
  - src/pages/practice/PronunciationDrillsPage.tsx
  - src/router/AppRouter.tsx
  - supabase/functions/ai-tutor/**/*
  - tests/regression/ai-tutor-golden-flow/**/*
  - tests/regression/golden-flow/**/*
  - docs/ai-tutor/AI_TUTOR_GOLDEN_FLOW_SPEC.md
  - scripts/run-ai-tutor-quality-gate.mjs
  - .gitlab-ci.yml

# Reusable rules: protected branch pushes run full validation regardless
# of changes. Merge requests use changes filters to skip heavy work for
# docs-only or unrelated updates.

.app_test_rules: &app_test_rules
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release(\/|$)/)'
      changes: *app_validation_changes

.lint_test_rules: &lint_test_rules
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release(\/|$)/)'
      changes: *lint_validation_changes

.room_test_rules: &room_test_rules
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release(\/|$)/)'
      changes: *room_validation_changes

.module_test_rules: &module_test_rules
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release(\/|$)/)'
      changes: *module_validation_changes

.ai_tutor_golden_flow_rules: &ai_tutor_golden_flow_rules
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
      changes: *ai_tutor_golden_flow_changes
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release(\/|$)/)'
      changes: *ai_tutor_golden_flow_changes

.manual_shared_app_rules: &manual_shared_app_rules
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
      when: manual
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release(\/|$)/)'
      changes: *app_validation_changes
      when: manual

.manual_shared_lint_rules: &manual_shared_lint_rules
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
      when: manual
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release(\/|$)/)'
      changes: *lint_validation_changes
      when: manual

.manual_shared_room_rules: &manual_shared_room_rules
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
      when: manual
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release(\/|$)/)'
      changes: *room_validation_changes
      when: manual

.manual_shared_module_rules: &manual_shared_module_rules
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
      when: manual
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release(\/|$)/)'
      changes: *module_validation_changes
      when: manual

.manual_shared_ai_tutor_golden_flow_rules: &manual_shared_ai_tutor_golden_flow_rules
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
      changes: *ai_tutor_golden_flow_changes
      when: manual
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release(\/|$)/)'
      changes: *ai_tutor_golden_flow_changes
      when: manual

.manual_shared_fallback: &manual_shared_fallback
  allow_failure: false

.build_script: &build_script
  - npx tsc --noEmit
  - npm run typecheck:functions
  # node:24-bookworm-slim ships without curl, the CA bundle, unzip, or
  # git. We need them for:
  #   - curl + ca-certificates: HTTPS fetch of the Deno install
  #     script and cert-chain validation (deno.land).
  #   - unzip: the Deno installer unpacks a .zip release ("Error:
  #     either unzip or 7z is required to install Deno" otherwise).
  #   - git: scripts/check-no-raw-react-lazy.mjs uses `git ls-files`
  #     as its fast path. Without git on PATH, the script falls back
  #     to a recursive walk() that has a latent `require()`-in-ESM
  #     bug — fixing the bug is out of scope for this CI port MR, so
  #     we keep the fast path live by installing git here. Other
  #     build-job steps that touch git (`git status --porcelain`,
  #     `git diff` in the registry-drift check) need it too.
  # `--no-install-recommends` keeps image bloat minimal; all four
  # packages are tiny. Only the `build` job needs Deno + the
  # registry-drift check, so the install scope is intentionally
  # limited here rather than hoisted into a shared anchor.
  - |
    case "$(uname -s)" in
      Darwin)
        command -v curl
        command -v unzip
        command -v git
        ;;
      Linux)
        apt-get update && apt-get install -y --no-install-recommends curl ca-certificates unzip git
        ;;
      *)
        echo "Unsupported CI OS: $(uname -s)" >&2
        exit 1
        ;;
    esac
  # Deno toolchain install for the edge-function check. The official
  # install.sh fetches the latest stable Deno when invoked without a
  # version arg. The repo's deno.lock is v5 → needs Deno >= 2.2; any
  # stable 2.x or later satisfies that. Important: do NOT pass
  # `v2.x` here. That works for the GitHub setup-deno action (which
  # does semver-range resolution) but install.sh interprets the arg
  # as a literal release tag and 404s on a non-existent `v2.x` tag.
  # If pinning becomes necessary, use a concrete release like
  # `v2.4.0`. On Linux/Docker, DENO_INSTALL=/usr/local preserves the
  # historical CI path. On macOS shell runners, install into the user's
  # writable home directory instead of /usr/local.
  - |
    if command -v deno >/dev/null 2>&1; then
      echo "Using existing Deno: $(command -v deno)"
    else
      case "$(uname -s)" in
        Darwin)
          export DENO_INSTALL="$HOME/.deno"
          ;;
        Linux)
          export DENO_INSTALL="/usr/local"
          ;;
        *)
          echo "Unsupported CI OS: $(uname -s)" >&2
          exit 1
          ;;
      esac
      curl -fsSL https://deno.land/install.sh | sh
    fi
  - export PATH="$HOME/.deno/bin:/usr/local/bin:$PATH"
  - command -v deno
  - npm run deno:check:functions
  - npm run build
  - node scripts/check-bundle-budget.mjs
  - node scripts/validate-data-files.js
  - node scripts/validate-room-registry.js
  - node scripts/validate-kids-rooms.js
  # check-delete-account-coverage exits 0 (skip) when Supabase env
  # vars are absent — safe to call unconditionally. When the CI/CD
  # variables VITE_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY are set
  # in GitLab project settings, the check fires for real.
  - node scripts/check-delete-account-coverage.mjs
  # Generate the room registry and fail if the on-disk version drifts
  # from what generation produces — mirrors the GitHub workflow's
  # "Generate and verify room registry" step.
  - echo "🔄 Generating room registry..."
  - node scripts/generate-room-registry.mjs
  - 'if [ -n "$(git status --porcelain src/lib/roomManifest.ts src/lib/roomDataImports.ts)" ]; then echo "❌ Registry files are out of sync — run scripts/generate-room-registry.mjs locally and commit."; git diff src/lib/roomManifest.ts src/lib/roomDataImports.ts; exit 1; fi'
  - echo "✅ Registry files are in sync"
  # NB: the Vitest suite no longer runs in `build`. It moved to the
  # sharded `test` jobs below (`npx vitest run --shard …`). `build` is
  # now typecheck + bundle build + data/registry validators only.

.lint_script: &lint_script
  - npm run lint
  - 'grep -r "console.log" src/ --exclude-dir=node_modules || true'

.validate_rooms_script: &validate_rooms_script
  - npm run validate-rooms

.module_boundaries_script: &module_boundaries_script
  - npm run depcruise:validate
  # `|| true` mirrors the GitHub workflow's `if: always()` + `|| true`.
  - npm run depcruise:metrics || true
  # Dead-code reaccumulation tripwire — INFO-LEVEL, never fails the pipeline.
  # Reports files that became orphans (no in/out edges) so a reviewer catches
  # them on the introducing PR. Full tiered inventory + false-positive classes:
  # docs/dead-code-risk-map.md. The `|| true` is belt-and-suspenders; the scan's
  # rules are severity:warn so depcruise already exits 0.
  - npm run depcruise:orphans || true

.test_shard_script: &test_shard_script
  # `node:24-bookworm-slim` ships without git. A few suites spawn it to
  # build throwaway repos — scripts/__tests__/audit-local-artifacts and
  # scripts/__tests__/check-manifest-delta both `git init`. When tests
  # ran inside the `build` job they inherited that job's apt-installed
  # git; the extracted shards must install it too or those suites fail
  # with `spawnSync git ENOENT` (whichever shard the files land in).
  # Only git is needed — curl/unzip/deno were build-only, not test deps.
  # `--no-install-recommends` keeps the install lean.
  - |
    case "$(uname -s)" in
      Darwin)
        command -v git
        ;;
      Linux)
        apt-get update && apt-get install -y --no-install-recommends git
        ;;
      *)
        echo "Unsupported CI OS: $(uname -s)" >&2
        exit 1
        ;;
    esac
  - npx vitest run --shard=$CI_NODE_INDEX/$CI_NODE_TOTAL

.ai_tutor_quality_gate_script: &ai_tutor_quality_gate_script
  - npm run test:ai-tutor-golden-flow

# ── `test` stage jobs ──────────────────────────────────────────────────

# `build` — typecheck + edge-fn typecheck + Deno check + bundle build +
# data/registry validators. The Vitest suite used to run here as a final
# `npm test` step; it now runs in the sharded `test` jobs below. The
# post-migration doc-drift detector
# (`tests/docs/post-migration-doc-drift.test.ts`) still runs because
# vitest discovers it under its default `tests/**` glob in those jobs.
#
# Mirrors GitHub's `build` job (minus the test step). Each line
# corresponds to a step there.
build:
  <<: [*node_job, *local_runner, *app_test_rules]
  stage: test
  script: *build_script

build:shared-fallback:
  <<: [*node_job, *manual_shared_fallback, *manual_shared_app_rules]
  stage: test
  script: *build_script

# `test` — Vitest suite, sharded across N parallel job instances.
# Extracted from the old monolithic `npm test` step that closed the
# `build` job. Vitest's native `--shard i/n` partitions test *files*
# across instances; GitLab's `parallel: 2` spawns two job copies and
# sets CI_NODE_INDEX (1..2) and CI_NODE_TOTAL (2). Each shard reports its
# own status and the pipeline passes only if BOTH shards pass.
#
# Profiled at 589 files / ~10173 tests: 2 shards split 295/294 files
# (~42s and ~49s, vs ~85s monolithic). On the `concurrent = 1` local
# runner the two shards still run serially, so today's wall-clock is
# roughly neutral (and pays one extra cached `npm ci`). The win is
# automatic the moment runner concurrency is raised or jobs route to
# cloud runners — bump `parallel` and the shards fan out to a ~2-4x
# speedup with no further repo change. Sharding by file means a flaky
# order-dependent test could surface differently than in the monolithic
# run; none were found (the union of `--shard 1/2` + `--shard 2/2`
# equals the monolithic run exactly — 589 files / 10173 tests, all
# green).
#
# Tests run against `src/` via vitest's own transform, so they do NOT
# need `npm run build` output and run independently of the `build` job.
# Cache CONSUMER (pull-only): shards share the node_modules cache the
# `build` producer seeds and never re-archive it. See `.npm_cache_def`.
test:
  <<: [*node_job, *local_runner, *app_test_rules]
  stage: test
  parallel: 2
  cache:
    <<: *npm_cache_def
    policy: pull
  script: *test_shard_script

test:shared-fallback:
  <<: [*node_job, *manual_shared_fallback, *manual_shared_app_rules]
  stage: test
  parallel: 2
  script: *test_shard_script

ai-tutor-quality-gate:
  <<: [*node_job, *local_runner, *ai_tutor_golden_flow_rules]
  stage: test
  cache:
    <<: *npm_cache_def
    policy: pull
  script: *ai_tutor_quality_gate_script

ai-tutor-quality-gate:shared-fallback:
  <<: [*node_job, *manual_shared_fallback, *manual_shared_ai_tutor_golden_flow_rules]
  stage: test
  script: *ai_tutor_quality_gate_script

delete-account-guard:
  <<: [*node_job, *local_runner]
  stage: test
  cache:
    <<: *npm_cache_def
    policy: pull
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
  script:
    - npm run check:delete-account-guard

delete-account-guard:shared-fallback:
  <<: [*node_job, *manual_shared_fallback]
  stage: test
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "merge_request_event"'
      when: manual
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
      when: manual
  script:
    - npm run check:delete-account-guard

delete-account-live:
  <<: [*node_job, *local_runner]
  stage: test
  allow_failure: true
  rules:
    - if: '$MERCYB_DELETE_ACCOUNT_LIVE_GUARD == "1" && $CI_PIPELINE_SOURCE == "push" && ($CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH =~ /^release(\/|$)/)'
      when: manual
    - if: '$MERCYB_DELETE_ACCOUNT_LIVE_GUARD == "1" && $CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release(\/|$)/)'
      changes:
        - scripts/check-delete-account-live.mjs
        - scripts/check-delete-account-coverage.mjs
        - supabase/functions/delete-account/**/*
        - .gitlab-ci.yml
      when: manual
  script:
    - node scripts/check-delete-account-live.mjs

# `lint` — eslint + a soft console.log warning (does not fail the job,
# same as GitHub's `continue-on-error: true`).
lint:
  <<: [*node_job, *local_runner, *lint_test_rules]
  stage: test
  # Cache consumer: pull only, never re-archive node_modules. See
  # `.npm_cache_def`.
  cache:
    <<: *npm_cache_def
    policy: pull
  script: *lint_script

lint:shared-fallback:
  <<: [*node_job, *manual_shared_fallback, *manual_shared_lint_rules]
  stage: test
  script: *lint_script

# `validate-rooms` — the full room-content validator. Heavier than the
# build-stage validate-room-registry / validate-kids-rooms steps; runs
# in its own job so a content-only break does not also stall the build.
validate-rooms:
  <<: [*node_job, *local_runner, *room_test_rules]
  stage: test
  # Cache consumer: pull only, never re-archive node_modules. See
  # `.npm_cache_def`.
  cache:
    <<: *npm_cache_def
    policy: pull
  script: *validate_rooms_script

validate-rooms:shared-fallback:
  <<: [*node_job, *manual_shared_fallback, *manual_shared_room_rules]
  stage: test
  script: *validate_rooms_script

# `module-boundaries` — dependency-cruiser validation. Per the GitHub
# workflow's note, errors are real violations; the metrics pass is
# informational and never fails.
module-boundaries:
  <<: [*node_job, *local_runner, *module_test_rules]
  stage: test
  # Cache consumer: pull only, never re-archive node_modules. See
  # `.npm_cache_def`.
  cache:
    <<: *npm_cache_def
    policy: pull
  script: *module_boundaries_script

module-boundaries:shared-fallback:
  <<: [*node_job, *manual_shared_fallback, *manual_shared_module_rules]
  stage: test
  script: *module_boundaries_script

# `check-new-orphans` — preventive, diff-scoped dead-code guard. Fails an MR
# that *introduces* a new orphan file (a src module nothing imports) or a new
# unused export, but never on the pre-existing backlog. Pure git + node: no
# npm ci, no module-graph build, no cache — just git (which the slim image
# lacks, so we apt-install it like the build job does). Typically ~2s of work.
#
# Runs on EVERY merge request (any target), not main-only — the point is to
# catch dead code at the PR that adds it. GIT_DEPTH 0 so the MR merge-base
# always resolves; the script fail-opens if it somehow cannot. See
# scripts/ci/check-new-orphans.mjs for the detection rules and FP guards.
.orphan_guard_script: &orphan_guard_script
  - |
    case "$(uname -s)" in
      Darwin)
        command -v git
        ;;
      Linux)
        apt-get update && apt-get install -y --no-install-recommends git
        ;;
      *)
        echo "Unsupported CI OS: $(uname -s)" >&2
        exit 1
        ;;
    esac
  - node scripts/ci/check-new-orphans.mjs

.orphan_guard_mr_rules: &orphan_guard_mr_rules
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'

check-new-orphans:
  <<: [*local_runner, *orphan_guard_mr_rules]
  image: node:24-bookworm-slim
  interruptible: true
  stage: test
  variables:
    # Full history so `git merge-base` against the MR target resolves even on
    # branches that diverged past the default shallow GIT_DEPTH.
    GIT_DEPTH: "0"
  cache: {}
  script: *orphan_guard_script

check-new-orphans:shared-fallback:
  image: node:24-bookworm-slim
  interruptible: true
  stage: test
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1" && $CI_PIPELINE_SOURCE == "merge_request_event"'
      when: manual
  variables:
    GIT_DEPTH: "0"
  cache: {}
  script: *orphan_guard_script

post-merge-worktree-reaper:
  <<: [*local_runner]
  interruptible: true
  stage: test
  tags:
    - local
    - mac
    - mercyb
    - isolated-builds
    - post-merge-reaper
  variables:
    GIT_DEPTH: "0"
  cache: {}
  rules:
    - if: '$MERCYB_SHARED_RUNNER_FALLBACK == "1"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
  script:
    - |
      set -euo pipefail
      REAPER_REPO="${MERCYB_REAPER_REPO:-}"
      if [ -z "$REAPER_REPO" ]; then
        for candidate in "${CI_PROJECT_DIR:-}" "$HOME/MercyB" /Users/admin/MercyB /Users/chaudoanm3/MercyB; do
          [ -n "$candidate" ] || continue
          if git -C "$candidate" rev-parse --git-dir >/dev/null 2>&1; then
            REAPER_REPO="$candidate"
            break
          fi
        done
      fi
      if [ -z "$REAPER_REPO" ]; then
        echo "[post-merge-worktree-reaper] no canonical local repo found; falling back to CI_BUILDS_DIR cleanup only"
        REAPER_REPO="$PWD"
      fi
      echo "[post-merge-worktree-reaper] resolved repo=$REAPER_REPO"
      git -C "$REAPER_REPO" fetch origin main --prune
      scripts/supervisor/post-merge-worktree-reaper.sh --live --repo "$REAPER_REPO" --merged-ref origin/main --roots "${MERCYB_REAPER_WORKTREE_ROOTS:-/private/tmp}" --builds-dir "${MERCYB_REAPER_BUILDS_DIR:-${CI_BUILDS_DIR:-}}" --reason "main-push pipeline ${CI_PIPELINE_ID:-unknown}"

# ── Manual production frontend deploy ─────────────────────────────────

deploy-netlify-publish-only:
  <<: [*node_job, *local_runner]
  stage: deploy
  allow_failure: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: manual
    - when: never
  script:
    - npm run build
    - npx --yes netlify-cli deploy --prod --dir=dist --no-build --site "$NETLIFY_SITE_ID" --auth "$NETLIFY_AUTH_TOKEN"

# ── `backup` stage (unchanged from prior version) ──────────────────────

# postgres:16-alpine ships pg_dump + pg_restore. We add gpg (gnupg) and
# rclone via apk so the same image carries the whole pipeline.
.backup_image: &backup_image
  image: postgres:16-alpine
  before_script:
    - apk add --no-cache --quiet gnupg rclone bash coreutils
    # Sanity check: the toolchain is present. Never echo env values.
    - command -v pg_dump
    - command -v gpg
    - command -v rclone
    - command -v bash

# Shared dump-and-upload body. Refer to scripts/db-backup/README.md for
# the rclone retention/prune commands that consume these date-bucketed
# folders.
.backup_script: &backup_script
  - mkdir -p "$BACKUP_OUTPUT_DIR"
  - export RCLONE_CONFIG="$RCLONE_CONFIG"
  # Run the dump script. It prints exactly two OK lines on success:
  #   OK <schema-file> <bytes>
  #   OK <full-file>   <bytes>
  - SUMMARY="$(bash scripts/db-backup/nightly-dump.sh)"
  - echo "$SUMMARY"
  # Pull the two file paths out of the OK summary lines.
  - SCHEMA_PATH="$(printf '%s' "$SUMMARY" | awk '/^OK /{print $2}' | sed -n '1p')"
  - FULL_PATH="$(printf '%s'   "$SUMMARY" | awk '/^OK /{print $2}' | sed -n '2p')"
  # Date-bucket the remote path so the rclone-prune commands in the
  # README can target daily / weekly / monthly subfolders cleanly:
  #   - daily/    every night
  #   - weekly/   on Sundays (day-of-week == 7 in ISO terms)
  #   - monthly/  on the 1st of the month
  - DOW="$(date -u +%u)"          # 1 (Mon) .. 7 (Sun)
  - DOM="$(date -u +%d)"          # 01..31
  - rclone copy --immutable "$SCHEMA_PATH" "$RCLONE_CONFIG_REMOTE/daily/"
  - rclone copy --immutable "$FULL_PATH"   "$RCLONE_CONFIG_REMOTE/daily/"
  - 'if [ "$DOW" = "7" ]; then rclone copy --immutable "$SCHEMA_PATH" "$RCLONE_CONFIG_REMOTE/weekly/"; rclone copy --immutable "$FULL_PATH" "$RCLONE_CONFIG_REMOTE/weekly/"; fi'
  - 'if [ "$DOM" = "01" ]; then rclone copy --immutable "$SCHEMA_PATH" "$RCLONE_CONFIG_REMOTE/monthly/"; rclone copy --immutable "$FULL_PATH" "$RCLONE_CONFIG_REMOTE/monthly/"; fi'
  - echo "Uploaded schema=$SCHEMA_PATH full=$FULL_PATH to $RCLONE_CONFIG_REMOTE"

# ── Nightly scheduled backup ───────────────────────────────────────────
#
# Required CI/CD variables (configure under Settings → CI/CD → Variables,
# all "protected", "masked" where the value format permits):
#
#   DATABASE_URL          masked     postgres connection string for prod
#                                    (or SUPABASE_DB_URL as fallback)
#   GPG_PUBLIC_KEY_FILE   file type  armored public key (multi-line file)
#                                    (or GPG_RECIPIENT_KEY_ID with a key
#                                    id if the keyring is preloaded — but
#                                    GitLab runners are fresh per job so
#                                    GPG_PUBLIC_KEY_FILE is the practical
#                                    choice in CI)
#   RCLONE_CONFIG         file type  contents of ~/.config/rclone/rclone.conf
#   RCLONE_CONFIG_REMOTE  protected  remote+path, e.g. "b2:mercyb-backups/prod"
#
# Both GPG_PUBLIC_KEY_FILE and RCLONE_CONFIG are GitLab "file"-type
# variables: GitLab writes their contents to a path inside the job
# workspace and exposes that path through the env var. Our dump script
# detects that GPG_PUBLIC_KEY_FILE is a file path and imports the key
# on the fly.

nightly-db-backup:
  <<: *backup_image
  stage: backup
  rules:
    # Schedule-only. Never runs on merge requests, pushes, or tags.
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
  variables:
    BACKUP_OUTPUT_DIR: "$CI_PROJECT_DIR/_backup_out"
    BACKUP_ENV_LABEL: "prod"
  script: *backup_script
  artifacts:
    # Keep the encrypted dumps as job artifacts for 7 days — a secondary
    # safety net behind the object-store upload. Dumps are encrypted, so
    # artifact visibility doesn't expose data.
    when: on_success
    expire_in: 7 days
    paths:
      - _backup_out/*.dump.gpg

# ── Manual "test the pipeline" job ─────────────────────────────────────
#
# Lets Chau trigger a one-off backup from the GitLab UI to validate the
# full pipeline without waiting for the schedule. It uses the same
# script and the same CI/CD variables as the scheduled job, so a green
# manual run means the schedule will also work.

test-db-backup-now:
  <<: *backup_image
  stage: backup
  rules:
    # Web (UI-triggered) pipelines only, AND manual gate.
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
  variables:
    BACKUP_OUTPUT_DIR: "$CI_PROJECT_DIR/_backup_out"
    BACKUP_ENV_LABEL: "prod"
  script: *backup_script
  artifacts:
    when: on_success
    expire_in: 7 days
    paths:
      - _backup_out/*.dump.gpg
